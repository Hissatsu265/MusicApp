import 'package:flutter/material.dart';

class AccoutTab extends StatelessWidget {
  const AccoutTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
